[package]
name = "worker"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
stellacomp = { path = "../stellacomp" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tonic = "0.12"

[build-dependencies]
tonic-build = "0.12"

